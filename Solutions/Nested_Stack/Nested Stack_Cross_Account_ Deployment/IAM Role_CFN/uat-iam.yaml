AWSTemplateFormatVersion: "2010-09-09"
Parameters:
  CentralAccountId:
    Type: String
    Default: '309967048489'
  CentralAccountRoleName:
    Type: String
    Default: CentralControlRole
  UatAccountRoleName:
    Type: String
    Default: UatAccessRole
Resources:
  DevAccountRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Ref UatAccountRoleName
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
        - Effect: Allow
          Principal:
            AWS: !Sub 'arn:aws:iam::${CentralAccountId}:role/${CentralAccountRoleName}'
          Action: sts:AssumeRole
      Path: /
      Policies:
      - PolicyName: CfnStackAssumeRole
        PolicyDocument:
          Version: '2012-10-17'
          Statement:
          - Effect: Allow
            Action: 
            - 'cloudformation:CreateStack'
            - 'cloudformation:UpdateStack'
            - 'cloudformation:DeleteStack'
            - 'cloudformation:DescribeStacks'
            Resource: "*"
          - Effect: Allow
            Action: "*"
            Resource: "*"
Outputs:
  DevAccountRoleArn:
    Value: !GetAtt UatAccountRole.Arn
